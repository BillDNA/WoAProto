# Obsidian workspace (local editor state)
.obsidian/

# dev harness dependencies (jsdom for dev/smoke.js)
dev/node_modules/
dev/package*.json

# Windows cruft
desktop.ini
Thumbs.db

# Art source binaries stay local (public repo, big files)
design-docs/ProtoArt/
design-docs/prototype pictures/*
# ...except the diagram the terrain rules are defined by
!design-docs/prototype pictures/HexClarificationDiagram.png
