You are the fresh, independent test-writer for #{TICKET} — woa-implement protocol phase 1 (`docs/woa-implement.md`). Read the ticket (`gh issue view {TICKET} --repo BillDNA/WoAProto`), work in `{WORKTREE}` on `{BRANCH}`, write each acceptance criterion's red-at-base falsifier at the home it names, prove each reds at base, and report the AC→test map (name any AC you can't write a binding red for). You do not implement and you do not open a PR.
