{
  "rulesVersion": "1.2",
  "tests": [
    "shapes (data-driven from maps.js)",
    "classic board (physical prototype, rows 4-5-6-5-4)",
    "human-readable grid labels",
    "custom board shapes (explicit hex sets)",
    "terrain pieces live inside ONE hex",
    "trench/terrain edge exclusivity",
    "multiple trenches per hex",
    "terrain attack table (A/B/C)",
    "combat math",
    "V0 terrain-crossing rules: trench support denial + rivers",
    "through-HQ movement & attacks",
    "deploy / control rules",
    "rules 1.1: a trench lets the defender survive a combat tie",
    "trench orientations are never fully off-board",
    "noOpener cards never in the opening hand (e.g. Airdrop)",
    "house rule: play any card as basic attack/reposition",
    "at least one step of a card must be played",
    "deck composition (data-driven from maps.js)",
    "army-points (WOA #54: computed from steps, weight table pinned)",
    "mispricing residual (WOA #57: cardRows points + residual, soft flag)",
    "army-points calibration pass (#82/#114: classify → class signal → cap-safe shared moves)",
    "deploy step budget vs stock (no deploy fallback, oversubscription = broken content)",
    "turn flow / first hand",
    "play metrics (seen / playLog for the card report)",
    "WOA-055 asymmetric deck binding",
    "noAdvance attacks (Ordered Withdraw holds its ground)",
    "Barrage targets ANY trench or forest",
    "no-op plays are logged and marked (skipped-turn report)",
    "deck legality lifted into the engine (#116): E.deckProblems shared by editor + drafter",
    "phase-0 LLM deckbuild (#116/#84, Track F): legal draft on the sideDecks path",
    "card catalog (#159): decks are refs, catalog is the one definition site",
    "map validation",
    "map pool",
    "index.html script-tag chain",
    "content/manifest.js staleness",
    "Field Marshal AI & skirmish sim",
    "attrition victory (surviving units on the board)",
    "behaviour counters (balance-lab metrics)",
    "metrics-v2 trace capture (per-play trace + units fold)",
    "AI personalities are data",
    "AI dead-turn regression (hard AI must not skip turn 1)",
    "concession",
    "concede advisory (foregone-conclusion heuristic)",
    "AI vs AI full matches",
    "fsTimeline: one [fsRed,fsBlue] pair per completed turn",
    "V1 AI search",
    "unit composition & values as content data",
    "report-model: bands as data + trace folds",
    "report-model: foldSkirmishes control% from hexesRed/hexesBlue",
    "report-model: cross-skirmish drill-down folds (Maps pane)",
    "chart-model: buildMapDrillModel (Maps pane display model)",
    "report-model: mapScoreDumbbells (Overview per-map balance fold)",
    "chart-model: ovFmt / ovTrackDomain / ovPos (Overview presentation math)",
    "chart-model: buildOverviewModel (Overview display model)",
    "report-model: Cards-pane folds (cardRunView / cardFleetFireTimes)",
    "chart-model: buildCardsModel (Cards pane display model)",
    "report-model: unitsAggFromRows (Units pane per-run fold)",
    "chart-model: buildUnitsModel + unLinearDomain/unPos (Units pane)",
    "loop-config: tolerance profiles parse + hard-flag Red%/1st%",
    "report-model: foldPanel takes worst-case per metric (never a mean), balance hard-flagged",
    "report-model: reportMarkdown surfaces calibratePoints suggestions (#156)",
    "loop-config: debrief questionnaire is an ordered id+text table with feel + reflex",
    "commanders content loads + schema",
    "invariant/determinism: same seed reproduces the skirmish",
    "invariant/parity: interactive step path equals the batch sim",
    "invariant/legality: every generated choice is a legal move",
    "invariant/terminal: AI-vs-AI always reaches a terminal state",
    "invariant/conservation: piece and terrain stocks stay finite",
    "invariant registry matches the labelled invariant set",
    "registry guard reds when a member is added to the registry",
    "registry guard reds when a labelled invariant is removed from the registry",
    "registry guard reds when a category strays from the frozen five",
    "every base-commit test is still present (deletion/skip is itself a red)",
    "deletion guard reds when a base test is removed with no bump",
    "deletion guard reds when a base test is .skip-ed with no bump",
    "deletion guard goes green when the removal rides a RULES_VERSION bump",
    "deletion guard goes green when the removal carries a pin-prune record"
  ],
  "prunedPins": []
}
