/* The field manual: the diagram player plus the text canon. Its body is markup
   (#manualContent in index.html), adopted at build rather than rebuilt in JS. */
'use strict';

uiModal({ id:'manual', title:'Field Manual', adopt:'manualContent' });
