/* War of Attrition — ui part: Field Manual. The manual is static markup in
   index.html (#manualOvr); opening/closing it is generic overlay wiring —
   $('btnManual') / $('btnManual2') onclick live in ui/boot.js and the Close
   button uses an inline onclick in the markup — so there is nothing to hold
   here yet. The V1 field-manual rule animations ([[v1-field-manual-animations]])
   land in this file. */
'use strict';
