# Engine vocabulary

Words for the rules engine and its machinery — the rules version, trace envelopes,
`balanceMap`, the golden-diff oracle, determinism, and GUI==CLI parity. The game concepts
it resolves (**Skirmish**, **Card**, **Map**) are anchors defined once in the root
[`CONTEXT.md`](../../CONTEXT.md) map.

*No terms carved yet.* This file is the home for engine vocabulary as it crystallizes.
Much of it lives today as prose in `docs/code-architecture.md`; move a term here when it
earns a glossary definition — one term, one canonical home.
