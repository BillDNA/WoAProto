# UI vocabulary

Words for the browser front-end — rendering primitives, the shared card face, screens,
and how the interface is drawn. The game-domain terms it renders (**Card**, **Map**,
**Deck**) are anchors defined once in the root [`CONTEXT.md`](../../CONTEXT.md) map.

*No terms carved yet.* This file is the home for UI vocabulary as it crystallizes —
including the UI-implementation terms from `docs/adr/0004-build-chain-teeth.md` (the base
primitive and register-or-extend rule; the existence / fidelity / readability split) once
that work lands. Add a term here the moment it is resolved, and only here — one term, one
canonical home.
