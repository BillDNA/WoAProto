# Sprint run — M1.1 · Measuring "good" (2026-07-16)

**Protocol:** full autonomous run (`run-sprint`, no bound — all 5 cards), strictly serial
(WOA-026/027/029 all rewrite `grading-rubrics.md`). One build sub-agent per card via the Workflow
primitive at the card's `Type:` model, xhigh effort, `TICKET_RESULT` schema. Runner-verified gates
(suite + wikilinks + real diff + numbers cross-checked against saved reports). No board hub this
session → direct `Sprint.md` edits. Steers batched up front (Step 0.5): WOA-029's 17-card call =
numbers → Bill decides; WOA-026's CLAUDE.md doctrine edit pre-approved with diff flagged.

**Pre-flight:** suite 237 green, wikilinks 0-dead (45 pages), tree clean at `edc4403`. No rig-notes
file (proceeded generic). No gitignored-dependency hits relevant to the cards (docs/rubric/skill
sprint; one measurement ticket).

## Scorecard — full run, every card dispositioned

| Card | Model | Outcome | cost (tok / time / tools) |
|---|---|---|---|
| WOA-025 drift sweep | sonnet | **closed** — 17/17 ACs, 20 files | 213k / 11.7m / 99 |
| WOA-026 re-baseline | sonnet | **closed** — every figure fresh, dated, setup-labeled | 145k / 30.4m / 64 |
| WOA-027 rubric refactor | opus | **closed** — Goal/Evidence/Score/Lever everywhere | 117k / 10.5m / 23 |
| WOA-028 review-reports refactor | sonnet | **closed** — + committed dry-run analysis | 141k / 7.4m / 30 |
| WOA-029 temp policy + T2 probe | opus | **closed** — recommendation HELD FOR BILL | 123k / 12.0m / 33 |

**Totals:** 5/5 dispatched-and-closed (nothing skipped/blocked — this was an unbounded run). Agent
spend ≈ 739k tokens / ~72 min sim+build / 249 tool uses. Zero bounces, zero blockers, zero
regressions — suite 237 → 237 at every gate; wikilinks 0-dead throughout. Commits `8b1c871`,
`8ade9e9`, `cb7e02e` (runner fix), `16951ac`, `b558cdb`, `c492f13` (+ this report's).

## Findings

1. **Cross-ticket staleness inside one run is real.** WOA-025 wrote skill gotcha-lines restating the
   then-current Behaviour baseline; WOA-026 moved that baseline hours later. Caught at WOA-027's gate
   by an anchor-integrity grep; fixed via a runner commit (run-tournament) + a dispatch fold-in
   (review-reports). Rule extracted: **when card N writes numbers a later card will move, write
   pointers** — and a runner grep of citing files at each gate is cheap insurance.
2. **Provenance > trust for historical figures.** WOA-026 discovered the old "normal n=40" Behaviour
   baseline was numerically a 12-map hard-vs-hard sweep — a recording-time mislabel. Structural fix
   shipped in WOA-027: the rubric now REQUIRES (AI tier, n, mapset, date) on every figure.
3. **The sprint closed its own loop live.** WOA-028's dry-run produced a T0 all-pass analysis — which
   WOA-029's policy then cites as the *worked example of the local-maximum signal*, and its T2 probe
   is the policy's first live application. The deliverables verified each other by position.
4. **Constraint-interplay finding (WOA-029):** WOA-017's `printed ≤ stock` test + the rubric's
   `printed ≥ stock` hard floor pin printed==stock; `cavsplit17-raid-paid` is engineered to that pin,
   so a 17-card deck does NOT breach the step-budget test.
5. **Honesty priming worked three times**: a confirmed-not-drift tie reading (WOA-026), a boring
   all-pass verdict defended as correct (WOA-028), and a Swings gain partially attributed to battle
   length as *inference, not measurement* (WOA-029).

## Held-over human steps (Bill)

1. **The 17-card call** — `cavsplit17-raid-paid` measured T0-clean on both tiers (Swings +25–30%,
   turtling down, +3 turns, normal 0-kill 1→4%). **Recommendation: ADOPT gated on one n≥100
   normal-tier confirmation run. Decision: yours** —
   `logs/reports/analysis/1.1/2026-07-16-1.1-analysis-cavsplit17-T2probe.md`. Say the word and the
   confirmation run happens.
2. **CLAUDE.md baselines diff** (pre-approved steer, flagged as promised): old row → first mover 45,
   Red 51, tie 9%, 6.3/5.4 atk/swp, 0-kill 1%, Drag 2.1, Swings 2.7 + skill premium now verified
   (69/73/56). Review at leisure; prior rows kept as superseded context.
3. **Behaviour-baseline mislabel** — the pre-WOA-020 "normal" figure was a hard-vs-hard read
   (flagged inline in the rubric, not retro-corrected). Acknowledge or ask for a deeper provenance
   sweep of older figures.
4. **Two rubric gaps (WOA-028 shakedown, yours to pin):** north star 1 (skill premium) is ungradeable
   unless a report set includes a `matchup` run — consider adding one to the standard `generate-reports`
   recipe; Game-level §2 Pacing has no pinned baseline (measured candidate: hard 28.7t / 23% HQ,
   normal 27.4t / 25%).
5. **Watch items** (all under-powered, no action implied): The Narrows mover-strong + hard Drag 2.6;
   Saber Ridge Red 57–58; The Marshes 1st 65 (normal only); The Narrows tie 23% under the candidate.

## Git-policy friction

- **PowerShell here-string commit messages break on embedded double quotes** (WOA-026's commit
  scattered the message into pathspecs — no bad commit landed, git errored cleanly). Switched to
  `git commit -F <scratchpad file>` for the rest of the run; recommend that as the standing recipe on
  this rig (platform lore — belongs in a future `rig-notes.md`, which this project doesn't have yet).
- Explicit-path staging + per-ticket commits worked cleanly throughout; one same-file case
  (Sprint.md carrying both the close-edit and the next flip) resolved naturally by commit ordering.

## Recommendations for these skills (refine-pass signal)

Routed per-ticket to canonical inbound as 5 observation drops (`from-warofattrition-observations-
WOA-02x-2026-07-16.md`). Highlights: pointer-not-numbers as a named feed-forward carry-class;
"measured AC returns actual numbers" schema field earning its keep; binding STEER blocks for
decision-shaped tickets; byte-identical baseline reproduction as standard for adopt/reject probes.
**Deviation, stated:** the closing `alignment-pass refine:<skill>` invocations were NOT auto-run
(consistent with the M1 run's deferred refine pass, which still stands un-asked-for) — the signal is
fully preserved in the observation drops + this report, and the combined M1+M1.1 refine pass remains
**offered, due on Bill's ask**. WoA-specific lore worth homing if a `rig-notes.md` is ever scaffolded:
`--parallel --once` sim chunking + timings, `--deck` in-memory selection, the printed==stock pin, the
orphan-process check after a foreground timeout.

## Verdict

**5/5 closed, zero bounces, the sprint's Done-when fully met pending Bill's review**: every rubric
figure is a dated Core Six measurement; every criterion is Goal/Evidence/Score/Lever-shaped;
review-reports grades temperature-stated with levers carried (proven by a committed dry-run); the
temperature policy exists and has settled the 17-card question **into a measured recommendation held
for Bill** — the one deliberate deviation from the ticket text, per the up-front steer. The sprint is
NOT closed on the board: Bill reviews (esp. held-over items 1–4), then `end-session close-sprint`.

#sprint-run
