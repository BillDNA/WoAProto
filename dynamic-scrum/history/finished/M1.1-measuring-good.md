# M1.1 · Measuring "good" — closed 2026-07-16

**Goal:** refine how "good" is defined and measured — sweep the M1 drift, re-measure every baseline
(rules 1.1 / Core Six), refactor `grading-rubrics.md` + `review-reports` around fresh figures, and
define the constraint-temperature search-side policy, settling the 17-card call under it.

**Ran:** 2026-07-16, fully autonomous (`run-sprint`, 5/5 closed, zero bounces, suite 237 green
throughout) · **Closed:** same day, after Bill's review. Run report:
`dynamic-scrum/planning/sprint-runs/2026-07-16-M1.1-measuring-good.md`.

**Headline:** the ruler is now current, shaped, and lever-naming — and its first live use settled a
real call: **Bill ADOPTED `cavsplit17-raid-paid`** (`D.D:seventeen-card-adopt`) off the T2 probe's
numbers (Swings +25–30%, turtling down, T0-clean both tiers). Execution gated on **WOA-030** — the
flip exposed card-pinned tests coupled to the active deck (suite crashed on the cut Ordered
Withdraw), so decouple + flip + atomic records ship together.

## Tickets

- **WOA-025 — Alignment M1 drift sweep** (Done): 17 ACs across 20 files — pointers, rules-era,
  gen-docs, tags, Roadmap-M1 reframe, stubs filled, drafts staging drained. 213k tok / 99 tools.
- **WOA-026 — Re-baseline under 1.1 / Core Six** (Done): every rubric figure re-measured, dated +
  setup-labeled; skill premium verified under 1.1 (n>e 69 / h>e 73 / h>n 56 thin); CLAUDE.md
  baselines updated; code-architecture numbers block → pointer. Finding: the old "normal n=40"
  Behaviour baseline was a mislabeled 12-map hard-vs-hard read. 145k tok / 64 tools.
- **WOA-027 — grading-rubrics refactor** (Done): Goal/Evidence/**Score/Lever** on all 23 criteria
  (25 lever statements, 3 justified "none — diagnostic"); §Temperature first-class; setup-label rule
  mandatory; superseded figures compacted out of grading positions. 117k tok / 23 tools.
- **WOA-028 — review-reports refactor** (Done): pointer-only grading, mandatory `Temperature: TX`
  header, lever carry-through, match-setup rule; proven by a committed T0 all-pass dry-run
  (`2026-07-16-1.1-analysis.md`) that cross-checked the LLM reserve felt-note against the WOA-016
  metric (doesn't reproduce in AI population → stays with WOA-024). Gaps reported: NS1 needs a
  matchup-shape report; Pacing baseline unpinned (candidate measured). 141k tok / 30 tools.
- **WOA-029 — search-side temperature policy + 17-card T2 probe** (Done): policy in the rubric slot
  (local-max signal = T0 all-pass + zero adoptable; ranked dial 16-card ceiling → stocks →
  deploy-steps with the printed==stock pin finding; re-measure-to-ship). Probe: byte-identical
  baseline re-runs, candidate T0-clean both tiers. Recommendation held per steer → **Bill adopted at
  close**. 123k tok / 33 tools.

Post-sprint addition (Bill): the **Quick reference table** — report line → target band, in report
print order — added to the rubric (`b2cb7bb`).

## Carried out of the sprint

- **WOA-030** (Backlog) — execute the adopt: decouple card-pinned tests, flip, atomic guardrail +
  baseline records, matchup re-measure (+ generate-reports matchup leg — the WOA-028 gap), n≥100
  0-kill confirmation as watch item.
- Pacing baseline candidate for Bill to pin (hard 28.7t/23% HQ, normal 27.4t/25% — pre-adopt deck).
- Combined M1+M1.1 canonical refine pass still offered, not run (signal in 5 observation drops +
  both run reports). `rig-notes.md` scaffold offered to home WoA sim lore.
- **Alignment pass flagged DUE** — this close flips Roadmap M1.1 Closed (milestone close).

## Related

[[Finished Index]] · [[Roadmap]] · [[Backlog]].

#finished
