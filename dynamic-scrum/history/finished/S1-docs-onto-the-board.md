# S1 · Docs onto the board — closed 2026-07-07

**Goal:** the board's docs layer is the ONE docs home — `design-docs/` dissolved into `dynamic-scrum/docs/`,
implemented specs indexed to orientation primers, root `CLAUDE.md` slimmed to doctrine.

**Dates:** opened 2026-07-07 (adopt session) · closed 2026-07-07.

**Done when:** `design-docs/` no longer exists, every mover's references are swept (code, skills, docs),
tests green, and Bill confirms. — **met.**

## Tickets

- **WOA-003 — Docs reorg: dissolve design-docs into dynamic-scrum/docs** (Done, 3 rounds, Bill confirmed):
  `design-docs/` + repo-root `specs/` dissolved — docs in `dynamic-scrum/docs/` (human-instructions divide
  kept), history in `dynamic-scrum/history/` (shipped-history + V0-summary), rubrics in
  `dynamic-scrum/rubrics/`, steam-roadmap parked, V0+V1 specs fully decomposed into docs and deleted
  (git = archive), `original-specs/` under `dynamic-scrum/planning/specs/`. CLAUDE.md slimmed to doctrine.
  All pinned paths swept same-commit (gen-docs, optimize-art, skills, code comments); dangling links fixed
  vault-wide; reports auto-tag `#reports #<kind> #v<version>` (+15 backfilled). Dogfood report sent to
  canonical. Tests/smoke/gen-docs green throughout.

## Related

[[Finished Index]] · [[Roadmap]].

#finished
